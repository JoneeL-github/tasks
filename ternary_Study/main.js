//ternary vs if else study below
let isSnowing = false;
let wheather;

if(isSnowing == true) {
  wheather = 'YES it is snowing'
} else {
  wheather = 'NO it is not snowing'
}
console.log(wheather);

let ternary = isSnowing ? 'Yes it is Snowing' : 'No it is not Snowing';
console.log(ternary);
